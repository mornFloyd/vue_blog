const baseUrl = 'http://localhost:3000/admin/api/rest/'
const categoryUrl = baseUrl+'categories'
const articleUrl = baseUrl+'articles'

const urlApi = {
  baseUrl,
  categoryUrl,
  articleUrl
}

export default urlApi
